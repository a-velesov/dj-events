import Layout from '@/components/Layout';

const Dashboard = () => {
    return (
        <Layout title="User Dashboard">
            <h1>Dashboard</h1>
        </Layout>
    );
};

export default Dashboard;